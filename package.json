{
  "name": "email-automation",
  "version": "2.6.2",
  "description": "Apps Script email automation for teacher weekly reports.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node test_runner.js",
    "pull": "clasp pull",
    "push": "clasp push",
    "open": "clasp open",
    "deploy": "npm test && clasp push && echo Deployed. Reload spreadsheet then run Email Tools - Run Unit Tests to confirm."
  }
}
